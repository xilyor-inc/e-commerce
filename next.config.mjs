/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // If you're deploying to a subdirectory (like GitHub Pages), uncomment and adjust the following:
  // basePath: '/hepsiburada-clone',
};

export default nextConfig;